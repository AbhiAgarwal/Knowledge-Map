<div class="row">
	<div class="span6 offset3">
		<div class="well">
	<%= image_tag @knowledge.image %>
	<p>
	  <b></b>
	  <%= @knowledge.description %>
	  <br>
	  By <%= @knowledge.user.name %>
	</p>
	<% if current_user == @knowledge.user %>
	<%= link_to 'Edit', edit_knowledge_path(@knowledge) %> |
	<% end %>
<%= link_to 'Back', knowledges_path %>
</div>
</div>
</div>

